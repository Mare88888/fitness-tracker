Highest-impact next additions

Program builder (goal-based plans): let users choose goal/days/equipment and auto-generate a 4-8 week plan.

Deload + fatigue management: auto-suggest lighter weeks when plateau/fatigue signals accumulate.

Progress photos (private): timestamped comparison with optional reminders; huge motivation feature.

Export/import backup: one-click CSV/JSON export and restore.




Product-quality essentials

Session/device management: show logged-in devices + revoke sessions.

Forgot/reset password + email verification: needed for real-user adoption.

Rate limiting + lockout policy on auth: protect against brute force.

Notifications/reminders: “planned workout today” + consistency nudges.

In-app onboarding/tutorial: first-time setup flow (goal, split, weekly target, preferred units).

Role-based admin tools: catalog moderation, duplicate merge, typo cleanup.




Analytics upgrades worth doing

Volume landmarks per muscle group (weekly target ranges).

Exercise trend cards (last 5 sessions, PR velocity, stagnation flags).

Bodyweight-adjusted strength metrics (e.g., estimated 1RM / bodyweight).

Template effectiveness insights (which routines produce best PR/streak outcomes).



