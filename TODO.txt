Data durability & trust
Real backup/restore (export/import JSON+CSV, scheduled DB backups) and soft-delete archive for workouts/templates.

Account maturity
Password reset, email verification, and “active sessions/devices” management with revoke.

Observability
Error tracking (Sentry), structured logs, and basic dashboards/alerts for auth failures, API latency, and DB health.

Progression intelligence
“Next best set” suggestions (progressive overload), plateau detection, and weekly goal adherence score.

Exercise model upgrade
First-class Exercise catalog in backend DB (id, aliases, primary muscle, secondary muscles), not just frontend constants.

Conflict-safe editing
Optimistic locking/versioning so concurrent edits don’t silently overwrite data across devices/tabs.